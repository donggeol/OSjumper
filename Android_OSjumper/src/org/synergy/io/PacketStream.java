
package org.synergy.io;


public class PacketStream  {

    /*public PacketStream (Stream stream) {
        this.stream = stream;

        EventQueue.getInstance ().removeHandlers (stream.getEventTarget ());
        EventQueue.getInstance ().adoptHandler (Event.UNKNOWN, stream.getEventTarget (),
                new EventJobInterface () { public void run (Event event) {
                    handleUpstreamEvent ();
                });

    }

    public void finalize () throws Throwable {
        EventQueue.getInstance ().removeHandler (Event.UNKNOWN, stream.getEventTarget ());
    }

    public void close () {
    }

    public Object getEventTarget () {
        return this;
    }

    private void handleUpstreamEvent () {
        // TODO
    }

    public 
    */
}
